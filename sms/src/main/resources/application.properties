# --- DATABASE CONFIGURATION ---
spring.application.name=sms
spring.jpa.hibernate.ddl-auto=update

# ADDED: These two lines prevent the database startup crash on Render
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver

# Updated to your NEW Neon Database (Direct Connection, No Pooler)
spring.datasource.url=jdbc:postgresql://ep-frosty-feather-ao5trrnv.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_zE7xM8uVdjFO

# --- GMAIL SMTP CONFIGURATION FOR PASSWORD RESET ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER}
spring.mail.password=${MAIL_PASSWORD}

# --- EXTRA SECURITY REQUIRED BY GMAIL ---
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com